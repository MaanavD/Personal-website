/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: process.env.SITE_URL || 'https://maanavdalal.com',
  generateRobotsTxt: true, // (optional)
  // REST CODE READ DOCS  ...
  }